import React from 'react';
import { TextInput, TextInputProps } from 'react-native-paper';

export default function TextInputAlwaysActivePlaceholder(props: TextInputProps) {
  return (
    <TextInput
      {...props}
    />
  );
}
